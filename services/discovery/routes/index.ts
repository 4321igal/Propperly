// Placeholder module — no implementation yet.
//
// R-1 extension: /api/discovery/* HTTP routes exposing DiscoveryApi (see
// ../CONTRACT.md) to APP. Real implementation per the Implementation
// Design, milestone M4. No new server, no new port — this mounts onto the
// existing services HTTP surface.

export function registerDiscoveryRoutes(): void {
  throw new Error("not implemented");
}
